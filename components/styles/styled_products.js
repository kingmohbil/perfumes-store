import styled from 'styled-components';

const Container = styled.div`
  grid-row: 2 / 4;
  max-height: 100%;
  padding: 50px;
  display: flex;
  flex-wrap: wrap;
  gap: 64px;
  overflow-y: auto;
`;

export default Container;
